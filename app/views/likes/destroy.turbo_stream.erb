<%= turbo_stream.replace "like-box" do %>  
  <div id="like-box">
    <% if @like_count > 0 %>
      <div>
        <%= @like_count %> people liked this post
      </div>
    <% end %>
    <%= link_to "like", post_likes_path(@post, Like.new), data: {turbo_method: :post} %>
  </div>
<% end %>